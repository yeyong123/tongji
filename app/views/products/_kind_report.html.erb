<div class="report-product">
	<span>当天机会类型报表</span>
	<% Kind.all.each do |kind| %>
		<% unless kind.advisories.today.count == 0 %>
			<div class="report-table product-today">
				<div class="product-number">
					<%= kind.advisories.today.count %>
				</div>
				<div class="product-name product-name2">
					<%= kind.name %>
				</div>
			</div>
		<% end %>
	<% end %>
</div>
<div class="report-product">
	<span>机会类型报表</span>
	<% Kind.all.each do |ind| %>
		<% unless ind.advisories.count == 0 %>
			<div class="report-table">
				<div class="product-number">
					<%= ind.advisories.count %>
				</div>
				<div class="product-name product-name2">
					<%= ind.name %>
				</div>
			</div>
		<% end %>
	<% end %>
</div>
